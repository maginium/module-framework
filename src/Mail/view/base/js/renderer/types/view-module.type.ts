import * as React from "react";

/**
 * Type definition for the imported view module.
 * It should export a default React functional component.
 */
export type ViewModule = {
  default: React.FC<any>;
};
