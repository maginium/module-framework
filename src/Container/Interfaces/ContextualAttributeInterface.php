<?php

declare(strict_types=1);

namespace Maginium\Framework\Container\Interfaces;

/**
 * Interface ContextualAttributeInterface.
 *
 * This interface represents a contextual attribute that can be associated with a
 * dependency during its resolution. It provides a mechanism for associating metadata
 * or contextual information with dependencies that can influence their resolution
 * or behavior.
 */
interface ContextualAttributeInterface
{
}
