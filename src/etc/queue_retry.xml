<?xml version="1.0" ?>
<config
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="urn:RunAsRoot:module:RunAsRoot_MessageQueueRetry:/etc/queue_retry.xsd"
>
    <!-- Retry limit configuration for event message topic -->
    <topic name="event.messages" retryLimit="3" />

    <!-- Retry limit configuration for email message topic -->
    <topic name="email.messages" retryLimit="3" />
</config>
